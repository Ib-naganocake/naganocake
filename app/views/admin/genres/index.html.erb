<div class="container mt-5">
  <p class="<%= flash[:color] %>"><%= flash[:notice] %></p>
  <div class="row">
    <h3 class="px-4" style="background: #f3f3f3">ジャンル一覧・追加</h3>
  </div>
  <div class="row text-center">
    <div class="col-6 ml-3 mt-5 mb-5">
      <%= render "new_form", genre: @genre %>
    </div>
    <div class="col-6">
      <%= render "index", genres: @genres %>
      <%= paginate @genres %>
    </div>
  </div>
</div>
