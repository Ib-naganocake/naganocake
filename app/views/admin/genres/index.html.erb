<div class="container mt-5">
  <p class="<%= flash[:color] %>"><%= flash[:notice] %></p>
  <div class="row">
    <div class="offset-1 text-center">
      <h3 class="px-4" style="background: #f3f3f3">ジャンル一覧・追加</h3>
    </div>
    <div class="col-6">
      <div class="row ml-3 mt-5 mb-5 text-center">
        <%= render 'form', genre: @genre %>
      </div>
      <div class="row text-center">
        <%= render 'index', genres: @genres %>
        <%= paginate @genres %>
      </div>
    </div>
  </div>
</div>
