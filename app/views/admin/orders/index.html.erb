<div class="container mt-5">
  <div class="row">
    <div class="offset-1 text-center">
      <h3 class="px-4" style="background: #f3f3f3"><%= "#{@customer.family_name} #{@customer.first_name}さんの注文履歴" %></h3>
    </div>
  </div>
  <div class="row mt-3">
    <div class="col-10 mx-auto">
      <table class="table">
        <thead style="background: #f3f3f3">
          <tr>
            <th>購入日時</th>
            <th>注文個数</th>
            <th>注文ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |orders| %>
            <tr>
              <td><u><%= link_to orders.created_at.strftime("%Y/%m/%d %H:%M:%S"), admin_order_path(orders), data: { turbolinks: false }, class: "text-reset" %></u></td>
              <td><%= calculate_order_total_amount(orders) %></td>
              <td><%= orders.transaction_status_i18n %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <%= paginate @orders %>
</div>